<% if user_signed_in? %>
    <% category = current_user.categories.where(name: "uncategorized").first%>
      <div class="btn btn-default"> <%= link_to 'New preference', new_brand_path %>
      </div><br>
      <div class="btn btn-default"> <%= link_to 'New category', new_user_category_path(current_user, Category.new) %>
      </div>
<% end %>
<div class="line"></div>
<% if user_signed_in? %>
  <%= render 'categories/category' %><br>
<div class="btn btn-default"> <%= link_to 'View all categories', user_categories_path(current_user) %>
      </div><br>
  <%= render 'preferences/preference' %>

<% end %>


